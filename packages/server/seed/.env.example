DATABASE_URL=postgresql://<user>:<pass>@localhost:5432/<db>

# Bootstrap admin credentials (used by seed) — no default password, must be set.
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_PASSWORD=changeme
