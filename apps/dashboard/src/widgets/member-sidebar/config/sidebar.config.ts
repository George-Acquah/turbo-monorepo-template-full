/** Cookie the server reads to render the correct sidebar width on first paint. */
export const SIDEBAR_COOKIE = 'app_sidebar';
export const SIDEBAR_COOKIE_MAX_AGE = 60 * 60 * 24 * 365;

/** Expanded / icon-rail widths. Kept here so the shell and sidebar agree. */
export const SIDEBAR_WIDTH = '16rem';
export const SIDEBAR_WIDTH_COLLAPSED = '4.5rem';
