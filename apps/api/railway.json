{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "docker/Dockerfile.API"
  },
  "deploy": {
    "startCommand": "pnpm --filter=@workspace/api run start:prod",
    "preDeployCommand": [
      "pnpm --filter=@workspace/prisma run migrate:deploy && if [ \"$SEED_AFTER_MIGRATE\" = \"true\" ]; then pnpm run db:seed; else echo 'Seeding skipped (SEED_AFTER_MIGRATE not true)'; fi"
    ],
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "numReplicas": 1
  }
}
